android {
    compileSdk = 34

    defaultConfig {
        applicationId = "com.receiptscan.ynab"
        minSdk = 18           // Required by flutter_secure_storage
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
            minifyEnabled = false
            shrinkResources = false
        }
    }
}

flutter {
    source = "../.."
}
